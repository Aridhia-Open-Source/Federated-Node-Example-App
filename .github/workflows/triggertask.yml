name: Update Task Definition
run-name: update_task
on:
  push:
    branches:
      - pipe-test

jobs:
  update:
    uses: Aridhia-Open-Source/federated-node-task-controller/.github/workflows/trigger-task.yml@gh-results
    with:
      DEST_BRANCH: argo-monitor
      TEMPLATE_PATH: argo/task.json
